# ── Convex DB (Required — same as other panels) ───────────────
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud

# ── JWT Secret (Must match all other panels exactly) ──────────
JWT_SECRET=your-random-32-char-secret-key-here

# ── App URLs (for cross-panel links) ─────────────────────────
NEXT_PUBLIC_STORE_URL=http://localhost:3000
NEXT_PUBLIC_ADMIN_URL=http://localhost:3001
NEXT_PUBLIC_DELIVERY_URL=http://localhost:3002
