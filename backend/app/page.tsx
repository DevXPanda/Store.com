export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>VegFru backend</h1>
      <p>API routes: <code>/api/auth/login</code>, <code>/api/auth/register</code>, <code>/api/orders</code></p>
    </main>
  );
}
